{
  "version": "1.0.1",
  "dependencies": {
    "mongoose": "^8.0.0"
  },
  "scripts": {
    "release": "npm version patch --no-git-tag-version && git add package.json && git commit -m \"Update to version $(node -p -e \"require('./package.json').version\")\" && git tag v$(node -p -e \"require('./package.json').version\") && git push && git push --tags"
  }
}
