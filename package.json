{
  "name": "magicwand-shared",
  "version": "1.0.13",
  "dependencies": {
    "mongoose": "^8.0.0"
  },
  "scripts": {
    "release": "npm version patch --no-git-tag-version && git add -A && git commit -m \"$npm_config_message\" && git tag v$(node -p -e \"require('./package.json').version\") && git push && git push --tags"
  }
}
