{
  "name": "magicwand-shared",
  "version": "1.0.35",
  "dependencies": {
    "mongoose": "^8.0.0"
  },
  "scripts": {
    "dev": "tsc",
    "build": "tsc",
    "release": "npm run build && git add -A && npm version patch --no-git-tag-version && git commit -m \"$npm_config_message\" && git tag v$(node -p -e \"require('./package.json').version\") && git push && git push --tags"
  },
  "description": "After making changes run: npm run release --message='Your commit message'",
  "types": "./dist/index.d.ts",
  "devDependencies": {
    "@types/node": "^20.11.17",
    "typescript": "^5.3.3"
  },
  "author": "",
  "license": "ISC"
}
